<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ezhealth">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EzHealth">
        <activity android:name=".TelaCadastro7"></activity>
        <activity android:name=".TelaCadastro6" />
        <activity android:name=".TelaCadastro5" />
        <activity android:name=".TelaCadastro4" />
        <activity android:name=".TelaCadastro3" />
        <activity android:name=".TelaCadastro2" />
        <activity android:name=".TelaCadastro1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>